# Live QA — 2026-08-31

This report records observed behavior from the local Cloudflare Pages runtime. It does not endorse the tested agent or claim a prize.

## End-to-end activation result

- All four required categories loaded in one refresh with 11 ranked entries.
- ERC-8004 token `120305` (Fabi) appeared in portfolio rebalancing from live 8004scan data.
- A direct BSC `ownerOf`/`tokenURI` read matched the indexed owner at observed block `119082844`.
- The public AgentCard bound itself to canonical BSC ERC-8004 token `120305` and declared `https://app.singularry.org/api/a2a` on the same HTTPS origin.
- One capability-only JSON-RPC `message/send` call returned the same ERC-8004 token, published agent wallet, canonical registry entry, and a portfolio/index capability.
- FourProof upgraded that single session from reachable to operational and generated a 30-minute read-only activation plan.
- The live proof index changed to 4/4 categories, 1 operational entry, and 10 safely blocked entries.

The request contained no wallet address, authentication, signature, funds, transaction, or instruction to trade.

## Adversarial checks

- A public AgentCard whose execution URL was `http://127.0.0.1:9101/` remained blocked.
- The client rejects plaintext, localhost, IP-literal, non-standard-port, and cross-origin execution URLs before sending a message.
- Oversized or non-JSON responses, mismatched request IDs, tokens, wallets, registries, or category capabilities remain blocked.
- Execution evidence is session-local and must be reproduced after a refresh; it is not cached as a permanent endorsement.

## Automated verification

- `npm run check`: passed.
- Vitest: 17 tests passed across four test files.
- TypeScript and Vite production build: passed.
- The existing bundle-size advisory remains non-blocking; no runtime or type error was observed in this flow.
